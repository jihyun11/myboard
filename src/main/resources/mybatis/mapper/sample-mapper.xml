<?xml version="1.0" encoding="euc-kr"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jihyun.myboard.mapper.SampleMapper">
    <select id="selectName" resultType="String">
        select dname from dept where deptno = 1;
    </select>

    <select id="selectPersnal" resultType="com.jihyun.myboard.entity.Sample">
        select name, phone, email, address from persnal where id = 1;
    </select>

    <insert id="insertPersnal" parameterType="String">
        UPDATE persnal
        SET name = #{name}, phone = #{phone}, email = #{email}, address = #{address}
        WHERE id = 1;
    </insert>
</mapper>